local Players = game:GetService("Players") local TeleportService = game:GetService("TeleportService") local HttpService = game:GetService("HttpService") local CoreGui = game:GetService("CoreGui") local Player = Players.LocalPlayer local PlaceId = game.PlaceId local guiParent = CoreGui if not guiParent then guiParent = Player:WaitForChild("PlayerGui") end local existingGui = guiParent:FindFirstChild("ServerBrowser") if existingGui then existingGui:Destroy() end local Gui = Instance.new("ScreenGui") Gui.Name = "ServerBrowser" Gui.ResetOnSpawn = false Gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling Gui.IgnoreGuiInset = true Gui.Parent = guiParent local Main = Instance.new("Frame") Main.Size = UDim2.fromOffset(190, 35) Main.Position = UDim2.new(0.5, -95, 0.5, -125) Main.BackgroundColor3 = Color3.fromRGB(25, 25, 30) Main.BorderSizePixel = 0 Main.ClipsDescendants = true Main.ZIndex = 999 Main.Parent = Gui Instance.new("UICorner", Main).CornerRadius = UDim.new(0, 6) local TitleBar = Instance.new("Frame") TitleBar.Size = UDim2.new(1, 0, 0, 35) TitleBar.BackgroundTransparency = 1 TitleBar.ZIndex = 999 TitleBar.Parent = Main local Title = Instance.new("TextLabel") Title.Size = UDim2.new(1, -60, 1, 0) Title.Position = UDim2.fromOffset(6, 0) Title.BackgroundTransparency = 1 Title.Text = "🌐 服务器" Title.TextColor3 = Color3.new(1, 1, 1) Title.TextSize = 11 Title.TextXAlignment = Enum.TextXAlignment.Left Title.Font = Enum.Font.GothamBold Title.ZIndex = 999 Title.Parent = TitleBar local Status = Instance.new("TextLabel") Status.Size = UDim2.new(1, -60, 1, 0) Status.Position = UDim2.fromOffset(6, 18) Status.BackgroundTransparency = 1 Status.Text = "就绪" Status.TextColor3 = Color3.fromRGB(180, 180, 180) Status.TextSize = 9 Status.TextXAlignment = Enum.TextXAlignment.Left Status.Font = Enum.Font.Gotham Status.ZIndex = 999 Status.Parent = TitleBar local Controls = Instance.new("Frame") Controls.Size = UDim2.fromOffset(44, 26) Controls.Position = UDim2.new(1, -50, 0, 4) Controls.BackgroundTransparency = 1 Controls.ZIndex = 999 Controls.Parent = TitleBar local ToggleBtn = Instance.new("TextButton") ToggleBtn.Size = UDim2.fromOffset(20, 20) ToggleBtn.Position = UDim2.fromOffset(0, 3) ToggleBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 70) ToggleBtn.Text = "−" ToggleBtn.TextColor3 = Color3.new(1, 1, 1) ToggleBtn.TextSize = 14 ToggleBtn.Font = Enum.Font.GothamBold ToggleBtn.ZIndex = 999 ToggleBtn.Parent = Controls ToggleBtn.AutoButtonColor = true Instance.new("UICorner", ToggleBtn).CornerRadius = UDim.new(0, 3) local CloseBtn = Instance.new("TextButton") CloseBtn.Size = UDim2.fromOffset(20, 20) CloseBtn.Position = UDim2.fromOffset(24, 3) CloseBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50) CloseBtn.Text = "X" CloseBtn.TextColor3 = Color3.new(1, 1, 1) CloseBtn.TextSize = 11 CloseBtn.Font = Enum.Font.GothamBold CloseBtn.ZIndex = 999 CloseBtn.Parent = Controls CloseBtn.AutoButtonColor = true Instance.new("UICorner", CloseBtn).CornerRadius = UDim.new(0, 3) local Content = Instance.new("Frame") Content.Size = UDim2.new(1, 0, 0, 0) Content.Position = UDim2.fromOffset(0, 35) Content.BackgroundTransparency = 1 Content.ClipsDescendants = true Content.ZIndex = 999 Content.Parent = Main local ButtonRow = Instance.new("Frame") ButtonRow.Size = UDim2.new(1, -8, 0, 28) ButtonRow.Position = UDim2.fromOffset(4, 4) ButtonRow.BackgroundTransparency = 1 ButtonRow.ZIndex = 999 ButtonRow.Parent = Content local Refresh = Instance.new("TextButton") Refresh.Size = UDim2.fromOffset(54, 22) Refresh.Position = UDim2.fromOffset(0, 3) Refresh.BackgroundColor3 = Color3.fromRGB(50, 120, 220) Refresh.Text = "刷新" Refresh.TextColor3 = Color3.new(1, 1, 1) Refresh.TextSize = 9 Refresh.Font = Enum.Font.GothamBold Refresh.ZIndex = 999 Refresh.Parent = ButtonRow Refresh.AutoButtonColor = true Instance.new("UICorner", Refresh).CornerRadius = UDim.new(0, 4) local ModeBtn = Instance.new("TextButton") ModeBtn.Size = UDim2.fromOffset(54, 22) ModeBtn.Position = UDim2.fromOffset(60, 3) ModeBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 70) ModeBtn.Text = "空服" ModeBtn.TextColor3 = Color3.new(1, 1, 1) ModeBtn.TextSize = 9 ModeBtn.Font = Enum.Font.GothamBold ModeBtn.ZIndex = 999 ModeBtn.Parent = ButtonRow ModeBtn.AutoButtonColor = true Instance.new("UICorner", ModeBtn).CornerRadius = UDim.new(0, 4) local List = Instance.new("ScrollingFrame") List.Size = UDim2.new(1, -8, 0, 120) List.Position = UDim2.fromOffset(4, 36) List.BackgroundColor3 = Color3.fromRGB(18, 18, 22) List.BorderSizePixel = 0 List.ScrollBarThickness = 3 List.CanvasSize = UDim2.new() List.ZIndex = 999 List.Parent = Content Instance.new("UICorner", List).CornerRadius = UDim.new(0, 4) local Layout = Instance.new("UIListLayout") Layout.Padding = UDim.new(0, 3) Layout.HorizontalAlignment = Enum.HorizontalAlignment.Center Layout.Parent = List local Padding = Instance.new("UIPadding") Padding.PaddingTop = UDim.new(0, 4) Padding.PaddingBottom = UDim.new(0, 4) Padding.Parent = List local isExpanded = false local filterMode = 1 local expandedHeight = 200 local collapsedHeight = 35 local allServers = {} local function UpdateSize() if isExpanded then Main.Size = UDim2.fromOffset(190, expandedHeight) Content.Size = UDim2.new(1, 0, 0, expandedHeight - 35) ToggleBtn.Text = "−" Status.Visible = true else Main.Size = UDim2.fromOffset(190, collapsedHeight) Content.Size = UDim2.new(1, 0, 0, 0) ToggleBtn.Text = "+" Status.Visible = false end end local function AddServer(server) local Row = Instance.new("Frame") Row.Size = UDim2.new(1, -6, 0, 32) Row.BackgroundColor3 = Color3.fromRGB(35, 35, 42) Row.BorderSizePixel = 0 Row.ZIndex = 999 Row.Parent = List Instance.new("UICorner", Row).CornerRadius = UDim.new(0, 4) local PlayersText = Instance.new("TextLabel") PlayersText.Size = UDim2.new(1, -56, 1, 0) PlayersText.Position = UDim2.fromOffset(5, 0) PlayersText.BackgroundTransparency = 1 PlayersText.TextXAlignment = Enum.TextXAlignment.Left PlayersText.Text = "👤" .. server.playing .. "/" .. server.maxPlayers PlayersText.TextColor3 = Color3.new(1, 1, 1) PlayersText.TextSize = 10 PlayersText.Font = Enum.Font.GothamMedium PlayersText.ZIndex = 999 PlayersText.Parent = Row local Join = Instance.new("TextButton") Join.Size = UDim2.fromOffset(44, 22) Join.Position = UDim2.new(1, -50, 0.5, -11) Join.BackgroundColor3 = Color3.fromRGB(50, 120, 220) Join.Text = "加入" Join.TextColor3 = Color3.new(1, 1, 1) Join.TextSize = 9 Join.Font = Enum.Font.GothamBold Join.ZIndex = 999 Join.Parent = Row Join.AutoButtonColor = true Instance.new("UICorner", Join).CornerRadius = UDim.new(0, 3) Join.MouseButton1Click:Connect(function() Join.Active = false Join.Text = "验证" Status.Text = "验证中..." local url = "https://games.roblox.com/v1/games/" .. PlaceId .. "/servers/Public?sortOrder=Asc&limit=100" local success, result = pcall(function() return HttpService:JSONDecode(game:HttpGet(url)) end) if success then for _, s in ipairs(result.data or {}) do if s.id == server.id then if s.playing < s.maxPlayers then Status.Text = "✅加入" TeleportService:TeleportToPlaceInstance(PlaceId, server.id, Player) else Status.Text = "❌已满" Join.Text = "已满" Join.BackgroundColor3 = Color3.fromRGB(180, 50, 50) task.wait(0.8) ScanServers() end return end end end Status.Text = "⚠️尝试中" TeleportService:TeleportToPlaceInstance(PlaceId, server.id, Player) end) end local function ClearList() for _, child in ipairs(List:GetChildren()) do if child:IsA("Frame") then child:Destroy() end end end local function RenderList() ClearList() local filtered = {} local maxPlayers = 1 if filterMode == 1 then maxPlayers = 1 elseif filterMode == 2 then maxPlayers = 2 end for _, server in ipairs(allServers) do if server.id ~= game.JobId then if server.playing <= maxPlayers and server.playing < server.maxPlayers then table.insert(filtered, server) end end end table.sort(filtered, function(a, b) return a.playing < b.playing end) if #filtered == 0 then local empty = Instance.new("TextLabel") empty.Size = UDim2.new(1, 0, 0, 32) empty.BackgroundTransparency = 1 empty.Text = filterMode == 1 and "无空服" or "无2人服" empty.TextColor3 = Color3.fromRGB(120, 120, 130) empty.TextSize = 10 empty.Font = Enum.Font.Gotham empty.ZIndex = 999 empty.Parent = List else for _, server in ipairs(filtered) do AddServer(server) end end List.CanvasSize = UDim2.new(0, 0, 0, Layout.AbsoluteContentSize.Y + 8) if filterMode == 1 then Status.Text = "空" .. #filtered else Status.Text = "2人" .. #filtered end end local function ScanServers() ClearList() Status.Text = "扫描中..." allServers = {} local cursor = nil local attempts = 0 repeat local url = "https://games.roblox.com/v1/games/" .. PlaceId .. "/servers/Public?sortOrder=Asc&limit=100" if cursor then url = url .. "&cursor=" .. HttpService:UrlEncode(cursor) end local success, result = pcall(function() return HttpService:JSONDecode(game:HttpGet(url)) end) if not success then attempts = attempts + 1 if attempts > 3 then Status.Text = "❌连接失败" break end Status.Text = "重试 " .. attempts task.wait(1) continue end attempts = 0 for _, server in ipairs(result.data or {}) do table.insert(allServers, server) end cursor = result.nextPageCursor task.wait(0.05) until not cursor if #allServers > 0 then RenderList() else Status.Text = "⚠️无服务器" local empty = Instance.new("TextLabel") empty.Size = UDim2.new(1, 0, 0, 32) empty.BackgroundTransparency = 1 empty.Text = "无可用服务器" empty.TextColor3 = Color3.fromRGB(120, 120, 130) empty.TextSize = 10 empty.Font = Enum.Font.Gotham empty.ZIndex = 999 empty.Parent = List end end local function ToggleMode() if filterMode == 1 then filterMode = 2 ModeBtn.Text = "2人" ModeBtn.BackgroundColor3 = Color3.fromRGB(50, 120, 220) else filterMode = 1 ModeBtn.Text = "空服" ModeBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 70) end RenderList() end Refresh.MouseButton1Click:Connect(function() Refresh.Active = false task.spawn(ScanServers) Refresh.Active = true end) ModeBtn.MouseButton1Click:Connect(function() ToggleMode() end) ToggleBtn.MouseButton1Click:Connect(function() isExpanded = not isExpanded UpdateSize() if isExpanded and #allServers == 0 then task.spawn(ScanServers) elseif isExpanded then RenderList() end end) CloseBtn.MouseButton1Click:Connect(function() Gui:Destroy() end) UpdateSize() task.spawn(function() task.wait(0.5) pcall(ScanServers) end)
