local Players = game:GetService("Players") local TeleportService = game:GetService("TeleportService") local HttpService = game:GetService("HttpService") local CoreGui = game:GetService("CoreGui") local Player = Players.LocalPlayer local PlaceId = game.PlaceId local guiParent = CoreGui if not guiParent then guiParent = Player:WaitForChild("PlayerGui") end local existingGui = guiParent:FindFirstChild("ServerBrowser") if existingGui then existingGui:Destroy() end local Gui = Instance.new("ScreenGui") Gui.Name = "ServerBrowser" Gui.ResetOnSpawn = false Gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling Gui.IgnoreGuiInset = true Gui.DisplayOrder = 10000000 Gui.Parent = guiParent local Main = Instance.new("Frame") Main.Size = UDim2.new(0, 158, 0, 25) Main.Position = UDim2.new(0.5, -79, 0.5, -150) Main.BackgroundColor3 = Color3.fromRGB(34, 34, 34) Main.BorderSizePixel = 0 Main.ClipsDescendants = true Main.Active = true Main.Draggable = true Main.Parent = Gui local TitleBar = Instance.new("Frame") TitleBar.Size = UDim2.new(0, 158, 0, 25) TitleBar.BackgroundColor3 = Color3.fromRGB(50, 50, 50) TitleBar.BorderSizePixel = 0 TitleBar.Parent = Main local Title = Instance.new("TextLabel") Title.Size = UDim2.new(0, 121, 0, 26) Title.Position = UDim2.new(0.112792775, 0, -0.0151660154, 0) Title.BackgroundTransparency = 1 Title.Text = "服务器" Title.TextColor3 = Color3.fromRGB(255, 255, 255) Title.TextSize = 25 Title.Font = Enum.Font.Sarpanch Title.Parent = TitleBar local CloseBtn = Instance.new("TextButton") CloseBtn.Name = "CloseButton" CloseBtn.Size = UDim2.new(0, 25, 0, 25) CloseBtn.Position = UDim2.new(1, -25, 0, 0) CloseBtn.Text = "X" CloseBtn.BackgroundColor3 = Color3.fromRGB(231, 76, 60) CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255) CloseBtn.Font = Enum.Font.GothamBold CloseBtn.TextSize = 16 CloseBtn.BorderSizePixel = 0 CloseBtn.Parent = Main local MinimizeBtn = Instance.new("TextButton") MinimizeBtn.Name = "MinimizeButton" MinimizeBtn.Size = UDim2.new(0, 25, 0, 25) MinimizeBtn.Position = UDim2.new(0, 0, 0, 0) MinimizeBtn.Text = "-" MinimizeBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 45) MinimizeBtn.TextColor3 = Color3.fromRGB(255, 255, 255) MinimizeBtn.Font = Enum.Font.GothamBold MinimizeBtn.TextSize = 22 MinimizeBtn.BorderSizePixel = 0 MinimizeBtn.Parent = Main local Content = Instance.new("Frame") Content.Size = UDim2.new(0, 158, 0, 0) Content.Position = UDim2.fromOffset(0, 25) Content.BackgroundTransparency = 1 Content.ClipsDescendants = true Content.Parent = Main local ButtonRow = Instance.new("Frame") ButtonRow.Size = UDim2.new(0, 146, 0, 32) ButtonRow.Position = UDim2.fromOffset(6, 6) ButtonRow.BackgroundTransparency = 1 ButtonRow.Parent = Content local Refresh = Instance.new("TextButton") Refresh.Size = UDim2.new(0, 121, 0, 32) Refresh.Position = UDim2.fromOffset(0, 0) Refresh.BackgroundColor3 = Color3.fromRGB(200, 200, 200) Refresh.Text = "刷新" Refresh.TextColor3 = Color3.fromRGB(0, 0, 0) Refresh.TextSize = 20 Refresh.Font = Enum.Font.SourceSansItalic Refresh.BorderSizePixel = 0 Refresh.Parent = ButtonRow local ModeBtn = Instance.new("TextButton") ModeBtn.Size = UDim2.new(0, 121, 0, 32) ModeBtn.Position = UDim2.fromOffset(0, 38) ModeBtn.BackgroundColor3 = Color3.fromRGB(200, 200, 200) ModeBtn.Text = "空服" ModeBtn.TextColor3 = Color3.fromRGB(0, 0, 0) ModeBtn.TextSize = 20 ModeBtn.Font = Enum.Font.SourceSansItalic ModeBtn.BorderSizePixel = 0 ModeBtn.Parent = ButtonRow local List = Instance.new("ScrollingFrame") List.Size = UDim2.new(0, 146, 0, 120) List.Position = UDim2.fromOffset(6, 82) List.BackgroundColor3 = Color3.fromRGB(25, 25, 30) List.BorderSizePixel = 0 List.ScrollBarThickness = 3 List.CanvasSize = UDim2.new() List.Parent = Content local Layout = Instance.new("UIListLayout") Layout.Padding = UDim.new(0, 4) Layout.HorizontalAlignment = Enum.HorizontalAlignment.Center Layout.Parent = List local Padding = Instance.new("UIPadding") Padding.PaddingTop = UDim.new(0, 4) Padding.PaddingBottom = UDim.new(0, 4) Padding.Parent = List local Status = Instance.new("TextLabel") Status.Size = UDim2.new(0, 146, 0, 18) Status.Position = UDim2.fromOffset(6, 206) Status.BackgroundTransparency = 1 Status.Text = "就绪" Status.TextColor3 = Color3.fromRGB(180, 180, 180) Status.TextSize = 11 Status.TextXAlignment = Enum.TextXAlignment.Left Status.Font = Enum.Font.Sarpanch Status.Parent = Content Status.Visible = false local isExpanded = false local filterMode = 1 local expandedHeight = 230 local collapsedHeight = 25 local allServers = {} local function UpdateSize() if isExpanded then Main.Size = UDim2.new(0, 158, 0, expandedHeight) Content.Size = UDim2.new(0, 158, 0, expandedHeight - 25) MinimizeBtn.Text = "-" Status.Visible = true else Main.Size = UDim2.new(0, 158, 0, collapsedHeight) Content.Size = UDim2.new(0, 158, 0, 0) MinimizeBtn.Text = "+" Status.Visible = false end end local function AddServer(server) local Row = Instance.new("Frame") Row.Size = UDim2.new(1, -8, 0, 34) Row.BackgroundColor3 = Color3.fromRGB(40, 40, 46) Row.BorderSizePixel = 0 Row.Parent = List local PlayersText = Instance.new("TextLabel") PlayersText.Size = UDim2.new(1, -60, 1, 0) PlayersText.Position = UDim2.fromOffset(8, 0) PlayersText.BackgroundTransparency = 1 PlayersText.TextXAlignment = Enum.TextXAlignment.Left PlayersText.Text = "👤 " .. server.playing .. "/" .. server.maxPlayers PlayersText.TextColor3 = Color3.fromRGB(255, 255, 255) PlayersText.TextSize = 11 PlayersText.Font = Enum.Font.Sarpanch PlayersText.Parent = Row local Join = Instance.new("TextButton") Join.Size = UDim2.fromOffset(46, 24) Join.Position = UDim2.new(1, -54, 0.5, -12) Join.BackgroundColor3 = Color3.fromRGB(200, 200, 200) Join.Text = "加入" Join.TextColor3 = Color3.fromRGB(0, 0, 0) Join.TextSize = 11 Join.Font = Enum.Font.SourceSansItalic Join.BorderSizePixel = 0 Join.Parent = Row Join.MouseButton1Click:Connect(function() Join.Active = false Join.Text = "验证" Status.Text = "验证中..." local url = "https://games.roblox.com/v1/games/" .. PlaceId .. "/servers/Public?sortOrder=Asc&limit=100" local success, result = pcall(function() return HttpService:JSONDecode(game:HttpGet(url)) end) if success then for _, s in ipairs(result.data or {}) do if s.id == server.id then if s.playing < s.maxPlayers then Status.Text = "✅ 加入" TeleportService:TeleportToPlaceInstance(PlaceId, server.id, Player) else Status.Text = "❌ 已满" Join.Text = "已满" Join.BackgroundColor3 = Color3.fromRGB(180, 50, 50) task.wait(0.8) ScanServers() end return end end end Status.Text = "⚠️ 尝试中" TeleportService:TeleportToPlaceInstance(PlaceId, server.id, Player) end) end local function ClearList() for _, child in ipairs(List:GetChildren()) do if child:IsA("Frame") or child:IsA("TextLabel") then child:Destroy() end end end local function RenderList() ClearList() local filtered = {} local maxPlayers = filterMode == 1 and 1 or 2 for _, server in ipairs(allServers) do if server.id ~= game.JobId then if server.playing <= maxPlayers and server.playing < server.maxPlayers then table.insert(filtered, server) end end end table.sort(filtered, function(a, b) return a.playing < b.playing end) if #filtered == 0 then local empty = Instance.new("TextLabel") empty.Size = UDim2.new(1, 0, 0, 32) empty.BackgroundTransparency = 1 empty.Text = filterMode == 1 and "无空服" or "无2人服" empty.TextColor3 = Color3.fromRGB(120, 120, 130) empty.TextSize = 12 empty.Font = Enum.Font.Sarpanch empty.Parent = List else for _, server in ipairs(filtered) do AddServer(server) end end List.CanvasSize = UDim2.new(0, 0, 0, Layout.AbsoluteContentSize.Y + 8) Status.Text = (filterMode == 1 and "空服" or "2人") .. " " .. #filtered end local function ScanServers() ClearList() Status.Text = "扫描中..." allServers = {} local cursor = nil local attempts = 0 repeat local url = "https://games.roblox.com/v1/games/" .. PlaceId .. "/servers/Public?sortOrder=Asc&limit=100" if cursor then url = url .. "&cursor=" .. HttpService:UrlEncode(cursor) end local success, result = pcall(function() return HttpService:JSONDecode(game:HttpGet(url)) end) if not success then attempts = attempts + 1 if attempts > 3 then Status.Text = "❌ 连接失败" break end Status.Text = "重试 " .. attempts task.wait(1) continue end attempts = 0 for _, server in ipairs(result.data or {}) do table.insert(allServers, server) end cursor = result.nextPageCursor task.wait(0.05) until not cursor if #allServers > 0 then RenderList() else Status.Text = "⚠️ 无服务器" local empty = Instance.new("TextLabel") empty.Size = UDim2.new(1, 0, 0, 32) empty.BackgroundTransparency = 1 empty.Text = "无可用服务器" empty.TextColor3 = Color3.fromRGB(120, 120, 130) empty.TextSize = 12 empty.Font = Enum.Font.Sarpanch empty.Parent = List end end local function ToggleMode() if filterMode == 1 then filterMode = 2 ModeBtn.Text = "2人" else filterMode = 1 ModeBtn.Text = "空服" end RenderList() end Refresh.MouseButton1Click:Connect(function() Refresh.Active = false task.spawn(ScanServers) Refresh.Active = true end) ModeBtn.MouseButton1Click:Connect(ToggleMode) MinimizeBtn.MouseButton1Click:Connect(function() isExpanded = not isExpanded UpdateSize() if isExpanded and #allServers == 0 then task.spawn(ScanServers) elseif isExpanded then RenderList() end end) CloseBtn.MouseButton1Click:Connect(function() Gui:Destroy() end) UpdateSize() task.spawn(function() task.wait(0.5) pcall(ScanServers) end)
