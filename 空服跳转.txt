local Players = game:GetService("Players") local TeleportService = game:GetService("TeleportService") local HttpService = game:GetService("HttpService") local CoreGui = game:GetService("CoreGui") local Player = Players.LocalPlayer local PlaceId = game.PlaceId local guiParent = CoreGui if not guiParent then guiParent = Player:WaitForChild("PlayerGui") end local existingGui = guiParent:FindFirstChild("ServerBrowser") if existingGui then existingGui:Destroy() end local Gui = Instance.new("ScreenGui") Gui.Name = "ServerBrowser" Gui.ResetOnSpawn = false Gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling Gui.IgnoreGuiInset = true Gui.Parent = guiParent local Main = Instance.new("Frame") Main.Size = UDim2.fromOffset(210, 38) Main.Position = UDim2.new(0.5, -105, 0.5, -150) Main.BackgroundColor3 = Color3.fromRGB(34, 34, 34) Main.BorderSizePixel = 0 Main.ClipsDescendants = true Main.ZIndex = 999 Main.Parent = Gui Instance.new("UICorner", Main).CornerRadius = UDim.new(0, 8) local TitleBar = Instance.new("Frame") TitleBar.Size = UDim2.new(1, 0, 0, 35) TitleBar.BackgroundColor3 = Color3.fromRGB(50, 50, 50) TitleBar.BorderSizePixel = 0 TitleBar.ZIndex = 999 TitleBar.Parent = Main Instance.new("UICorner", TitleBar).CornerRadius = UDim.new(0, 8) TitleBar.Active = true TitleBar.Draggable = true local Title = Instance.new("TextLabel") Title.Size = UDim2.new(1, -60, 1, 0) Title.Position = UDim2.fromOffset(12, 0) Title.BackgroundTransparency = 1 Title.Text = "🌐 服务器" Title.TextColor3 = Color3.new(1, 1, 1) Title.TextSize = 16 Title.TextXAlignment = Enum.TextXAlignment.Left Title.Font = Enum.Font.Sarpanch Title.ZIndex = 999 Title.Parent = TitleBar local Controls = Instance.new("Frame") Controls.Size = UDim2.fromOffset(50, 28) Controls.Position = UDim2.new(1, -56, 0, 3) Controls.BackgroundTransparency = 1 Controls.ZIndex = 999 Controls.Parent = TitleBar local ToggleBtn = Instance.new("TextButton") ToggleBtn.Size = UDim2.fromOffset(22, 22) ToggleBtn.Position = UDim2.fromOffset(0, 3) ToggleBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 70) ToggleBtn.Text = "−" ToggleBtn.TextColor3 = Color3.new(1, 1, 1) ToggleBtn.TextSize = 16 ToggleBtn.Font = Enum.Font.Sarpanch ToggleBtn.ZIndex = 999 ToggleBtn.Parent = Controls ToggleBtn.AutoButtonColor = true Instance.new("UICorner", ToggleBtn).CornerRadius = UDim.new(0, 4) local CloseBtn = Instance.new("TextButton") CloseBtn.Size = UDim2.fromOffset(22, 22) CloseBtn.Position = UDim2.fromOffset(27, 3) CloseBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50) CloseBtn.Text = "X" CloseBtn.TextColor3 = Color3.new(1, 1, 1) CloseBtn.TextSize = 12 CloseBtn.Font = Enum.Font.Sarpanch CloseBtn.ZIndex = 999 CloseBtn.Parent = Controls CloseBtn.AutoButtonColor = true Instance.new("UICorner", CloseBtn).CornerRadius = UDim.new(0, 4) local Content = Instance.new("Frame") Content.Size = UDim2.new(1, 0, 0, 0) Content.Position = UDim2.fromOffset(0, 35) Content.BackgroundTransparency = 1 Content.ClipsDescendants = true Content.ZIndex = 999 Content.Parent = Main local ButtonRow = Instance.new("Frame") ButtonRow.Size = UDim2.new(1, -12, 0, 32) ButtonRow.Position = UDim2.fromOffset(6, 6) ButtonRow.BackgroundTransparency = 1 ButtonRow.ZIndex = 999 ButtonRow.Parent = Content local Refresh = Instance.new("TextButton") Refresh.Size = UDim2.fromOffset(56, 24) Refresh.Position = UDim2.fromOffset(0, 4) Refresh.BackgroundColor3 = Color3.fromRGB(50, 120, 220) Refresh.Text = "刷新" Refresh.TextColor3 = Color3.new(1, 1, 1) Refresh.TextSize = 12 Refresh.Font = Enum.Font.Sarpanch Refresh.ZIndex = 999 Refresh.Parent = ButtonRow Refresh.AutoButtonColor = true Instance.new("UICorner", Refresh).CornerRadius = UDim.new(0, 4) local ModeBtn = Instance.new("TextButton") ModeBtn.Size = UDim2.fromOffset(56, 24) ModeBtn.Position = UDim2.fromOffset(64, 4) ModeBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 70) ModeBtn.Text = "空服" ModeBtn.TextColor3 = Color3.new(1, 1, 1) ModeBtn.TextSize = 12 ModeBtn.Font = Enum.Font.Sarpanch ModeBtn.ZIndex = 999 ModeBtn.Parent = ButtonRow ModeBtn.AutoButtonColor = true Instance.new("UICorner", ModeBtn).CornerRadius = UDim.new(0, 4) local List = Instance.new("ScrollingFrame") List.Size = UDim2.new(1, -12, 0, 130) List.Position = UDim2.fromOffset(6, 44) List.BackgroundColor3 = Color3.fromRGB(25, 25, 30) List.BorderSizePixel = 0 List.ScrollBarThickness = 3 List.CanvasSize = UDim2.new() List.ZIndex = 999 List.Parent = Content Instance.new("UICorner", List).CornerRadius = UDim.new(0, 4) local Layout = Instance.new("UIListLayout") Layout.Padding = UDim.new(0, 4) Layout.HorizontalAlignment = Enum.HorizontalAlignment.Center Layout.Parent = List local Padding = Instance.new("UIPadding") Padding.PaddingTop = UDim.new(0, 4) Padding.PaddingBottom = UDim.new(0, 4) Padding.Parent = List local Status = Instance.new("TextLabel") Status.Size = UDim2.new(1, -12, 0, 18) Status.Position = UDim2.fromOffset(6, 4) Status.BackgroundTransparency = 1 Status.Text = "就绪" Status.TextColor3 = Color3.fromRGB(180, 180, 180) Status.TextSize = 11 Status.TextXAlignment = Enum.TextXAlignment.Left Status.Font = Enum.Font.Sarpanch Status.ZIndex = 999 Status.Parent = ButtonRow Status.Visible = true local isExpanded = false local filterMode = 1 local expandedHeight = 230 local collapsedHeight = 38 local allServers = {} local function UpdateSize() if isExpanded then Main.Size = UDim2.fromOffset(210, expandedHeight) Content.Size = UDim2.new(1, 0, 0, expandedHeight - 35) ToggleBtn.Text = "−" Status.Visible = true else Main.Size = UDim2.fromOffset(210, collapsedHeight) Content.Size = UDim2.new(1, 0, 0, 0) ToggleBtn.Text = "+" Status.Visible = false end end local function AddServer(server) local Row = Instance.new("Frame") Row.Size = UDim2.new(1, -8, 0, 34) Row.BackgroundColor3 = Color3.fromRGB(40, 40, 46) Row.BorderSizePixel = 0 Row.ZIndex = 999 Row.Parent = List Instance.new("UICorner", Row).CornerRadius = UDim.new(0, 4) local PlayersText = Instance.new("TextLabel") PlayersText.Size = UDim2.new(1, -60, 1, 0) PlayersText.Position = UDim2.fromOffset(8, 0) PlayersText.BackgroundTransparency = 1 PlayersText.TextXAlignment = Enum.TextXAlignment.Left PlayersText.Text = "👤 " .. server.playing .. "/" .. server.maxPlayers PlayersText.TextColor3 = Color3.new(1, 1, 1) PlayersText.TextSize = 11 PlayersText.Font = Enum.Font.Sarpanch PlayersText.ZIndex = 999 PlayersText.Parent = Row local Join = Instance.new("TextButton") Join.Size = UDim2.fromOffset(46, 24) Join.Position = UDim2.new(1, -54, 0.5, -12) Join.BackgroundColor3 = Color3.fromRGB(50, 120, 220) Join.Text = "加入" Join.TextColor3 = Color3.new(1, 1, 1) Join.TextSize = 11 Join.Font = Enum.Font.Sarpanch Join.ZIndex = 999 Join.Parent = Row Join.AutoButtonColor = true Instance.new("UICorner", Join).CornerRadius = UDim.new(0, 4) Join.MouseButton1Click:Connect(function() Join.Active = false Join.Text = "验证" Status.Text = "验证中..." local url = "https://games.roblox.com/v1/games/" .. PlaceId .. "/servers/Public?sortOrder=Asc&limit=100" local success, result = pcall(function() return HttpService:JSONDecode(game:HttpGet(url)) end) if success then for _, s in ipairs(result.data or {}) do if s.id == server.id then if s.playing < s.maxPlayers then Status.Text = "✅ 加入" TeleportService:TeleportToPlaceInstance(PlaceId, server.id, Player) else Status.Text = "❌ 已满" Join.Text = "已满" Join.BackgroundColor3 = Color3.fromRGB(180, 50, 50) task.wait(0.8) ScanServers() end return end end end Status.Text = "⚠️ 尝试中" TeleportService:TeleportToPlaceInstance(PlaceId, server.id, Player) end) end local function ClearList() for _, child in ipairs(List:GetChildren()) do if child:IsA("Frame") then child:Destroy() end end end local function RenderList() ClearList() local filtered = {} local maxPlayers = filterMode == 1 and 1 or 2 for _, server in ipairs(allServers) do if server.id ~= game.JobId then if server.playing <= maxPlayers and server.playing < server.maxPlayers then table.insert(filtered, server) end end end table.sort(filtered, function(a, b) return a.playing < b.playing end) if #filtered == 0 then local empty = Instance.new("TextLabel") empty.Size = UDim2.new(1, 0, 0, 32) empty.BackgroundTransparency = 1 empty.Text = filterMode == 1 and "无空服" or "无2人服" empty.TextColor3 = Color3.fromRGB(120, 120, 130) empty.TextSize = 12 empty.Font = Enum.Font.Sarpanch empty.ZIndex = 999 empty.Parent = List else for _, server in ipairs(filtered) do AddServer(server) end end List.CanvasSize = UDim2.new(0, 0, 0, Layout.AbsoluteContentSize.Y + 8) Status.Text = (filterMode == 1 and "空服" or "2人") .. " " .. #filtered end local function ScanServers() ClearList() Status.Text = "扫描中..." allServers = {} local cursor = nil local attempts = 0 repeat local url = "https://games.roblox.com/v1/games/" .. PlaceId .. "/servers/Public?sortOrder=Asc&limit=100" if cursor then url = url .. "&cursor=" .. HttpService:UrlEncode(cursor) end local success, result = pcall(function() return HttpService:JSONDecode(game:HttpGet(url)) end) if not success then attempts = attempts + 1 if attempts > 3 then Status.Text = "❌ 连接失败" break end Status.Text = "重试 " .. attempts task.wait(1) continue end attempts = 0 for _, server in ipairs(result.data or {}) do table.insert(allServers, server) end cursor = result.nextPageCursor task.wait(0.05) until not cursor if #allServers > 0 then RenderList() else Status.Text = "⚠️ 无服务器" local empty = Instance.new("TextLabel") empty.Size = UDim2.new(1, 0, 0, 32) empty.BackgroundTransparency = 1 empty.Text = "无可用服务器" empty.TextColor3 = Color3.fromRGB(120, 120, 130) empty.TextSize = 12 empty.Font = Enum.Font.Sarpanch empty.ZIndex = 999 empty.Parent = List end end local function ToggleMode() if filterMode == 1 then filterMode = 2 ModeBtn.Text = "2人" ModeBtn.BackgroundColor3 = Color3.fromRGB(50, 120, 220) else filterMode = 1 ModeBtn.Text = "空服" ModeBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 70) end RenderList() end Refresh.MouseButton1Click:Connect(function() Refresh.Active = false task.spawn(ScanServers) Refresh.Active = true end) ModeBtn.MouseButton1Click:Connect(ToggleMode) ToggleBtn.MouseButton1Click:Connect(function() isExpanded = not isExpanded UpdateSize() if isExpanded and #allServers == 0 then task.spawn(ScanServers) elseif isExpanded then RenderList() end end) CloseBtn.MouseButton1Click:Connect(function() Gui:Destroy() end) UpdateSize() task.spawn(function() task.wait(0.5) pcall(ScanServers) end)
